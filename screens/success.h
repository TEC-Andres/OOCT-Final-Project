#ifndef SCREENS_SUCCESS_H
#define SCREENS_SUCCESS_H

#include <windows.h>

// Render the success screen with same background styling
void runSuccessScreen(HANDLE hIn, HANDLE hOut);

#endif // SCREENS_SUCCESS_H
